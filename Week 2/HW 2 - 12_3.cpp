#include <iostream>
using namespace std;

template <typename T>
T binarySearch(const T list[], int key, int listSize) {
  int low = 0;
  int high = listSize - 1;

  while (high >= low) {
    int mid = (low + high) / 2;
    if (key < list[mid]) {

    }
  }
}

int main() {

}
